# Loaded by Vite when `bun run build` is run (production mode).
# Local dev uses `.env` or falls back to http://localhost:8000 (see src/lib/api.ts).
# Safe to commit — this is a public API URL, not a secret.
VITE_API_BASE_URL=https://backend-production-70f9.up.railway.app
